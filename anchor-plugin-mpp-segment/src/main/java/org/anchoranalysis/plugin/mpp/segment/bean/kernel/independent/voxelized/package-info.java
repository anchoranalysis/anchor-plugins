/**
 * Implementations of {@link
 * org.anchoranalysis.plugin.mpp.segment.bean.kernel.independent.KernelBirth} that are
 * <b>independent</b> but use the voxelized state of a {@link org.anchoranalysis.mpp.mark.Mark}.
 *
 * <p>The voxelized state requires computation to calculate, so these kernels are typically are more
 * computationally expensive than other kernels.
 */
package org.anchoranalysis.plugin.mpp.segment.bean.kernel.independent.voxelized;
