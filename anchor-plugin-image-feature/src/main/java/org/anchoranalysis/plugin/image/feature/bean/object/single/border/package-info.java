/**
 * Implementations of {@link
 * org.anchoranalysis.image.feature.bean.object.single.FeatureSingleObject} that determine if an
 * object lies at the border of an image.
 *
 * <p>The border is the exterior of the image, the boundary at which voxels have no neighbors in at
 * least one direction.
 */
package org.anchoranalysis.plugin.image.feature.bean.object.single.border;
